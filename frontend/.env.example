# Optional. If unset, the dashboard calls the API on the same host it is served
# from, port 8000 (works for localhost and LAN-IP access). Set this only to
# point the dashboard at a specific backend URL.
# VITE_API_BASE=http://localhost:8000
